BOTH OPERATIONS
- Make QTY text colour green if more than 1
- When using app to put item on PO, use the SKU being ordered in the note left
- if last name is "NOLASTNAME", make field blank
- have notes save/refresh when removing order from list, going back, or marking as sent. This is just in case a user types the message without saving it.
- show a confirmation that note is updating/has been updated

AFTERNOON OPERATIONS
- fix Bonds couriers label booking
- add a "dev mode" or debug view so I can keep track of bugs from within the EXE
- make default date range smaller (1 month)

DAILY OPERATIONS
- need to add a way to search APIC if item is not found in musipos inventory, ask the user if they want to import it into inventory, and use the SKU to order.
- add method to update SKU location
- research if it is possible to show if a user is already processing an order; this may require some kind of user management system
- refresh order when opening it
- loading the daily orders current session from the shortcut opens the afternoon session
- make picking sheet have alternating background colours for each row for easier readability

SETTINGS menu
- Add to launcher menu
- Include a way to manage users (this will carry over into POS operations)
- include a way to assign orders to users
- show users if someone else is currently processing an order
